spino-install-1password:
    #!/usr/bin/env bash
    brew trust ublue-os/tap
    brew tap ublue-os/tap
    brew install --cask 1password-gui-linux
    
spino-install-framework-tool:
    #!/usr/bin/env bash
    brew trust ublue-os/tap
    brew tap ublue-os/tap
    brew install --cask framework-tool
